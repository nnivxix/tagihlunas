import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.tagihlunas.app",
  appName: "tagihlunas",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
